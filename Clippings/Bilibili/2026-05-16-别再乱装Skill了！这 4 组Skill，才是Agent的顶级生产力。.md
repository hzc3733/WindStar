---
title: "别再乱装Skill了！这 4 组Skill，才是Agent的顶级生产力。"
url: "https://www.bilibili.com/video/BV1RcRmBbE3x/?spm_id_from=333.1007.tianma.4-1-11.click&vd_source=be52938c173d1c63babcec4f036a9ee0"
bvid: "BV1RcRmBbE3x"
cid: "38197201623"
author: "Juang_42号搭车客"
upload_date: "2026-05-08"
subtitle_lang: "中文"
created: "2026-05-16"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116540062041120&bvid=BV1RcRmBbE3x&cid=38197201623&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

如果Skill没有配置好，那么Agent使用起来的效果就会事倍功半。
我尝试了十几个Agent和几十个Skill，这些Skill是我每天都在用的，而且不依赖特定的Agent。
无论你是在用 Claude Code 还是 Codex，这套通用的 Skill 组合都能让你的 AI 助手瞬间进化。

## 章节

- `00:00` 技能配置决定生产力
- `00:41` 核心Skill
- `02:13` 软件开发
- `07:01` 前端设计
- `09:13` 内容创作
- `12:21` 总结与建议

## 字幕

### 技能配置决定生产力 `00:00`

`00:00` 别再纠结是CODX还是cloud code了
`00:02` 我在实测了数十个agent之后
`00:04` 发现真正决定生产力上限的不仅仅是agent的工具
`00:07` 还有你手里的skill配置
`00:09` 如果你的skill没配对
`00:10` 换再强的agent也是在浪费时间
`00:13` 所以我根据实际开发场景和我的日常使用
`00:16` 筛选出了这四组最核心的顶级skill
`00:19` 涵盖了原能力扩展工程化开发
`00:21` 前端设计和内容创作
`00:23` 他们完全不挑平台
`00:24` 不管你以后切换到哪个agent的装上都能用
`00:27` 先讲最根本的两把钥匙
`00:29` 我称为原s skill
`00:30` 你可以把它理解成让AI自我进化的能力
`00:33` 他不负责具体的活
`00:34` 而是专门用来扩展agent的能力边界的
`00:37` 不管你用AI做什么
`00:39` 这都是你第一天就应该打好的地基

### 核心Skill `00:41`

`00:41` 第一个是skill crater
`00:43` 来自ANTHROPIC官方
`00:44` 如果你想把一套成熟的工作流
`00:46` 变成一个新的skill
`00:47` 便于后续调用
`00:48` 那么选它就对了
`00:50` 以前想自己做个skill特别麻烦
`00:52` 得先去研究半天复杂的格式
`00:54` 不然可能写出来的skill还会报错
`00:56` 就算写出来了
`00:57` 使用效果也不一定尽如人意
`00:59` 但现在有了它
`01:00` 你不需要去研究什么复杂的格式
`01:02` 也不用手动改文件
`01:03` 你只需要像给同事交代工作一样
`01:06` 用大白话把你的流程说一遍
`01:08` 或者直接把你的操作手册丢给它
`01:10` 它就会自动帮你起草测试
`01:12` 反复迭代
`01:13` 在你自己完全不用看开发文档的情况下
`01:15` 一分钟就能写出一个既标准又好用的skill
`01:18` 安装和使用方式也很简单
`01:20` 在安装完成后
`01:21` 只需要在agent里选中skill creator
`01:24` 然后输入你的需求
`01:25` 和他一步步的进行沟通就好
`01:27` 建议直接全局安装
`01:29` 这样无论你在哪个项目里都可以随时进行调用
`01:32` 第二个是find skills
`01:33` 大家千万别把它当成一个普通的搜索插件
`01:36` 觉得还得自己手动去查
`01:38` 真正的用法是你直接给agent的拍任务就行了
`01:41` 比如你让他帮你做个UI设计
`01:43` 要是他发现自己不会
`01:44` 它就会自动把你的需求拆解成ui design这种关键词
`01:48` 然后自己去全网搬救兵
`01:50` 他在后台连接的是skill
`01:51` 点SH这个平台
`01:52` 他会自己查看哪个skill安装量大
`01:55` 哪个作者靠谱
`01:56` 然后挑出最好的那个供你进行选择
`01:58` 在你选择好之后
`02:00` 它还能直接一行命令帮你安装上skill
`02:02` creator是让他能自己造工具
`02:04` 而final skills是让他能去外面找现成的
`02:07` 这两个配合使用
`02:08` 一定能大大提升你的agent的工作效率
`02:11` 接下来是针对具体场景的skill

### 软件开发 `02:13`

`02:13` 先说软件开发
`02:15` 我选了这三个super powers
`02:17` j stack和一个前端大神的skill
`02:19` 他们针对的场景略有区别
`02:21` 但核心都在解决同一个问题
`02:23` 就是终结那种看似逻辑闭环
`02:25` 实则无法落地的代码幻觉
`02:27` 帮你守住工程底线
`02:28` 第一个super powers
`02:30` 他的杀手锏在于
`02:31` 他把测试驱动开发这套严苛的工程标准
`02:34` 直接变成了agent必须遵守的硬规则
`02:37` 其实很多人刚开始用AI编程
`02:39` 最容易上手的场景就是让他写测试
`02:41` 而super powers顺着这个逻辑
`02:43` 直接把开发流程给正规化了
`02:46` 他会强制agent进入一套标准的红绿重构循环
`02:49` 先写一个必然失败的测试
`02:51` 证明功能还没实现
`02:52` 然后写最少量的代码
`02:53` 让它变绿
`02:54` 最后再进行优化
`02:55` 而且它非常稳agent的
`02:57` 写完之后
`02:58` 它会自动开启两轮内部审计
`03:00` 一轮看代码实现跟你的需求对不对得上
`03:02` 另一轮则专门盯着代码的质量挑毛病
`03:05` 这种慢思考的模式
`03:06` 能帮你抓出很多隐藏的边界问题
`03:08` 虽然看起来多花了一点点时间
`03:10` 但因为他第一遍就能把代码写到80分以上
`03:13` 省掉了后面无数次反复debug的时间
`03:15` 长期来看反而更省
`03:17` token也更省钱
`03:18` 他的整个工作流程大致如下
`03:20` 首先他会拉着你做头脑风暴
`03:22` 把需求细节彻底摸清楚
`03:24` 先出一份整体的设计文档
`03:26` 然后他会把大任务拆成一个个
`03:28` 几分钟就能搞定的小碎活
`03:30` 每个活儿都有明确的验证标准
`03:32` 接着就是让子A着呢
`03:33` 自己去跑
`03:34` 他自己写
`03:35` 自己查
`03:35` 严禁跳步
`03:37` 你只要在旁边关键节点确认一下就行
`03:41` 他会把选项丢给你
`03:42` 是直接合并代码
`03:43` 还是先留着分支
`03:45` 或者觉得不行
`03:46` 直接丢掉
`03:46` 第二个是g stack
`03:48` 作者是YC的总裁GARY谭
`03:50` 如果你还不知道YC是什么
`03:52` 简单说他就是全球最牛的创业孵化器
`03:55` 像AIRBNBDROPBOX这种巨头都是他孵化出来的
`03:58` 所以这位大佬出的工具
`04:00` 骨子里带的就是那种硅谷创业者的实战基因
`04:02` 这个工具有一点不同
`04:04` 他不是那种功能单一的skill
`04:06` 而是在agent里内置了23个不同的专家角色
`04:09` 从CEO设计师到发布工程师
`04:12` 你都可以通过斜杠命令直接调用
`04:14` 这相当于给agent配齐了一整支团队
`04:17` 让他不再是单兵作战
`04:18` 为什么要搞这么多角色
`04:20` 因为真正做商业系统代码行数不值钱
`04:23` 能跑通才值钱
`04:24` 有了这群专家帮你交叉审计
`04:26` agent就能在不同的专业视角下
`04:28` 帮你揪出那些隐藏极深的问题
`04:30` 我来向你介绍一下它的实战流程
`04:33` 首先在你动手写第一行代码之前
`04:35` 先跑一下office hours命令
`04:37` 这就是YC最出名的灵魂拷问
`04:39` AI不会立刻写代码
`04:40` 而是像个严厉导师一样
`04:42` 反问你六个最尖锐的问题
`04:44` 把不靠谱的假设先掐死
`04:46` 接着可以用plan CEO review命令
`04:48` 让agent站在CEO的高度审视计划
`04:51` 看看有没有更优解到了代码复核阶段
`04:54` review命令就是你的资深工程师
`04:56` 他不光找小bug
`04:57` 更盯着那些CII能过
`04:59` 但一上线就可能爆炸的工程隐患
`05:01` 另一个具有实战特色的是QA命令
`05:04` 以前agent呢只能在代码里纸上谈兵
`05:06` 但这个命令是真的会打开浏览器
`05:08` 像真人测试员一样去点击验证
`05:11` 直接把bug抓出来修掉
`05:12` 最后活干完了
`05:13` 直接执行shape命令
`05:15` 它会自动同步跑测试
`05:17` 推代码开P2
`05:18` 整套发布动作一气呵成
`05:20` 盖瑞谈统计过2026年
`05:22` 它的代码产出是2013年的240倍
`05:24` 这不是说AI写的代码行数多就是厉害
`05:26` 而是同样的需求
`05:28` 他一个人现在能顶一支小团队在干活
`05:30` 这就是角色分工带来的本质变化
`05:32` 第三个是一套前端大神mart
`05:35` 自己日常工作用的skill
`05:36` 作者是typescript的布道者
`05:38` 如果你平时前端开发比较多
`05:40` 那么可以试试这个这套工具
`05:42` 重点解决的是人与agent之间沟通对不齐的问题
`05:45` mart总结过
`05:46` 如果没有好的引导规则
`05:48` agent的写代码很容易陷入几种困境
`05:50` 首先是理解偏差
`05:51` agent呢可能根本没听懂你需要什么
`05:53` 或者写的太啰嗦
`05:55` 废话很多
`05:55` 然后是执行失败
`05:57` 好不容易写出来的代码
`05:58` 结果发现根本跑不通
`05:59` 最后是架构隐患
`06:01` 虽然代码能跑
`06:02` 但因为缺乏整体规划
`06:03` 后期维护起来会非常痛苦
`06:05` 所以他的这套skill核心逻辑很简单
`06:07` 宁可在前期多花几分钟对其需求
`06:10` 也不要在后期花几个小时去处理
`06:12` 这套低质量的代码
`06:13` 具体到这套skill里面的指令
`06:15` 我建议你重点关注这几个
`06:17` 首先是grill me系列的命令
`06:19` 这就是刚才提到的拷问模式
`06:21` 当你提了一个模糊的需求
`06:22` 比如说想加个登录功能
`06:24` 他不会马上动手
`06:25` 而是会回过头来不停的拷问你细节
`06:28` 可能问完之后
`06:28` 他发现你真正想要的是
`06:30` SS环境下的多租户登陆
`06:32` 这就把隐患消灭在开工之前了
`06:35` 接着是垂直命令
`06:36` 也就是issue分诊
`06:37` 他会帮你把所有的任务都过一遍
`06:40` 分清楚轻重缓急
`06:41` 确保你不是在修一些细枝末节的小bug
`06:44` 而忽视了真正阻塞进度的核心问题
`06:47` 最后还有一个improve命令
`06:48` 这是代码库的架构急救包
`06:50` 你可以每隔几天就跑一次
`06:52` 让agent的站在全局的视角审视你的代码库
`06:55` 找出那些以后可能会越来越难改的地方
`06:58` 并给出重构的建议
`06:59` 接下来是前端页面设计

### 前端设计 `07:01`

`07:01` 这是最开始编程A阵的出来时
`07:03` 他做的最差的一个领域之一
`07:05` agent的话
`07:05` UI出来的永远都是那些固定的套路
`07:08` 固定的字体
`07:09` 蓝紫色的渐变背景
`07:10` 圆角卡片
`07:11` 特定的按钮样式
`07:12` 你在网上看到的那些AI生成的界面
`07:15` 十个里面有12个长得都一样
`07:17` 解决这个问题的skill有两个
`07:19` 第一个是front end design anthropic官方出品
`07:22` 如果你受够了那种千篇一律的AI审美
`07:25` 那他就是你的救星
`07:26` 以前的AI画UI
`07:27` 一眼看过去全是圆角
`07:29` 卡片加紫色渐变
`07:30` 就像是在共用一套廉价的模板
`07:32` 而front end design的核心是帮你洗掉这些AI way
`07:35` 它不是机械地套用组件
`07:37` 而是根据你的产品调性去推敲更有质感的纹理
`07:40` 或者尝试那种更有呼吸感的非对称布局
`07:43` 比如你给他提一个具体的风格要求
`07:45` 想要一个杂质感
`07:47` 带点硬核感的页面
`07:48` 他给出的方案里
`07:49` 字体的比例和模块间的留白都会处理的很到位
`07:52` 有了这种对视觉细节的把控
`07:55` 你的UI就从一眼AI变成了手作设计
`07:57` 让整个页面从单纯的能看
`08:00` 变成了真正意义上的耐看
`08:02` 第二个是ui ux pro max
`08:04` 如果说前面的工具是帮你找灵感
`08:06` 那这个就是直接帮你配了一个设计总监
`08:09` 他的特点在于它不是在靠直觉画图
`08:12` 而是把专业设计的那些条条框框
`08:14` 全部变成了底层的逻辑
`08:16` 比如你要做一个金融或者医疗类的界面
`08:19` 他会非常明确地告诉你
`08:20` 什么样的配色能体现安全感
`08:22` 什么样的字体更显专业
`08:24` 他甚至还会给你列出一份避坑指南
`08:27` 直接点出哪些设计
`08:28` 在商业场景里是绝对不能碰的
`08:30` 之所以能这么专业
`08:31` 是因为它后台内置了
`08:33` 160多个行业的深度规则
`08:35` 不管你遇到多冷门的业务
`08:37` 他都能拿出一套成体系的方案
`08:39` 从交互细节到动效走位都给你安排的明明白白
`08:43` 而且它有一个很实在的功能
`08:45` 就是能帮你生成一套可以持久化复用的
`08:47` 设计系统
`08:48` 有了这套规范
`08:49` 你下次再开发新项目
`08:51` 直接把文件丢给agent就能用
`08:53` 不用每次都从零开始打磨风格
`08:55` 而且他的上手门槛很低
`08:57` 无论是装插件还是用命令行都能快速跑起来
`09:00` 这两款工具的分工也很明确
`09:02` front end的design负责把画面画得出彩
`09:04` 而UIUXPROMAX负责把产品做得更专业
`09:08` 有了它们AI的输出
`09:09` 就再也不会有那种廉价感了
`09:11` 最后一类内容创作

### 内容创作 `09:13`

`09:13` 如果你用agent做内容创作
`09:15` 那这组宝玉老师的skill我一定要强力推荐给你
`09:18` 他首先解决的就是内容本身的高质量产出
`09:21` 比如它能帮你生成一张极具审美
`09:24` 完全不输专业设计师的封面图
`09:26` 或者把一大段枯燥的文字
`09:28` 直接变成一张高信息密度的可视化信息图
`09:31` 在内容做漂亮之后
`09:32` 他还会顺手帮你搞定后面那些讨厌的碎活儿
`09:35` 比如说转格式做排版
`09:37` 最后还能直接一键发布到各个平台
`09:40` 他把从生产到发布的全流程都打通了
`09:43` 有了它
`09:43` 你就能真正实现生产和发布一体化
`09:46` 把所有的精力都集中在打磨好内容上
`09:49` 宝玉老师的这套工具箱里
`09:50` 包含了十几个好用的skill
`09:52` 我这里简单带大家看几个
`09:54` 首先是用于生成封面图的cover image skill
`09:57` 它最强的地方在于有一套五维控制系统
`09:59` 从构图类型
`10:01` 色调方案
`10:02` 渲染风格到文字排版和情绪
`10:04` 基调全都能精准调优
`10:07` 这77种预设组合
`10:08` 能让封面彻底告别开盲盒的随机感
`10:11` 每一张出来的效果
`10:12` 都像是为你的文章量身定制的专业设计
`10:15` 如果你平时觉得画逻辑图架构图很头疼
`10:18` 那这个信息图相关的skill绝对是神器
`10:21` 它内置了21种专业的信息布局
`10:23` 像分析原因的鱼骨图
`10:25` 做转化的漏斗图
`10:27` 梳理层级的金字塔图应有尽有
`10:30` 更聪明的是
`10:31` 它能自动读懂你文案里的逻辑结构
`10:33` 直接推荐最合适的布局方案
`10:35` 以前要在设计软件里磨半天的信息大图
`10:38` 现在只需要几秒钟
`10:40` 就能产出出版级的可视化成果
`10:42` 如果你经营小红书
`10:44` 那么可以使用小红书image skill
`10:46` 它能将长文章自动拆解为1~10张
`10:49` 卡通风格的轮播卡片
`10:50` 通过内置的11种视觉风格和八种排版模式
`10:54` 如对比清单流程等
`10:56` 可以快速生成符合平台排版习惯的图文内容
`10:59` 针对排版环节
`11:01` 这个markdown to HTML的skill
`11:03` 解决的是一个非常具体的痛点
`11:05` 那就是在微信公众号这种不支持markdown的平台上
`11:09` 如何保留精致的排版
`11:10` 它内置了多套公众号主题
`11:12` 能自动处理代码高亮和数学公式
`11:15` 最实用的一点是
`11:16` 它能把文中的普通外链
`11:18` 自动转为文墨的底部引用
`11:20` 彻底解决了公众号里链接打不开
`11:22` 或者被截断的尴尬
`11:24` 如果你平时还有翻译文章
`11:25` 或者精读外文资料的需求
`11:27` 那这个翻译skill就派上用场了
`11:29` 它最强的地方在于
`11:31` 提供了一个正式出版集的模式
`11:33` 这个模式不是直接进行翻译
`11:36` 而是会走分析
`11:37` 翻译校正再到润色这整整四步的流程
`11:40` 而且它还有一个非常人性化的功能
`11:43` 就是能让你指定你的读者是谁
`11:45` 比如你告诉他
`11:46` 你的读者是资深开发者
`11:48` 它就会自动省略掉那些冗余的解释
`11:50` 翻译出来的语气
`11:52` 读起来就像是真正的圈内人写的
`11:54` 最后当你把内容全部准备妥当
`11:56` 可以通过发布微信或者发布微博
`11:58` 这两个skill来实现一键跨平台分发
`12:01` 它区分了不同的分发逻辑
`12:03` 你可以发长文形式的文章
`12:05` 也可以选择只发几张图片
`12:07` 配一段摘要的贴图模式
`12:09` 他把那些复杂的后台操作
`12:11` 全都变成了agent里面的一行指令
`12:13` 从本地草稿到最终发布
`12:15` 整套流程都可以在agent里面直接闭环完成
`12:18` 今天分享的这些skill只是个开始

### 总结与建议 `12:21`

`12:21` 其实最关键的是
`12:22` 大家要根据自己的工作流程和使用场景
`12:25` 去打磨出真正适合自己的skill
`12:27` 如果觉得视频对你有帮助
`12:29` 别忘了点赞和订阅
`12:30` 我是君
`12:30` 望
`12:30` 我们下期再见