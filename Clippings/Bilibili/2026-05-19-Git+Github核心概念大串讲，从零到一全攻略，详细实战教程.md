---
title: "Git+Github核心概念大串讲，从零到一全攻略，详细实战教程"
url: "https://www.bilibili.com/video/BV1ySLc6QEcB/?spm_id_from=333.1007.tianma.3-1-7.click&vd_source=be52938c173d1c63babcec4f036a9ee0"
bvid: "BV1ySLc6QEcB"
cid: "38425857864"
author: "技术爬爬虾"
upload_date: "2026-05-18"
subtitle_lang: "中文"
created: "2026-05-19"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116595242307520&bvid=BV1ySLc6QEcB&cid=38425857864&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

本期带来一个Git 与 GitHub 核心概念大串讲，我会使用动画讲解结合实战演示的方式，把所有的核心概念一次讲清楚。我们先从本地Git仓库开始，介绍提交，分支，分区等核心概念。然后引入远端仓库，详细介绍
 Github网站的基础用法，包括怎么在Github中进行多人协作，如何在开源项目中贡献代码。

## 章节

- `00:00` 引言
- `00:50` Git与GitHub是什么
- `03:57` 实战准备
- `08:29` Commit提交
- `14:18` Branch分支
- `17:50` WorkTree
- `20:52` Git分区
- `23:26` 远端仓库实战
- `27:33` GitHub入门
- `32:29` GitHub多人协作

## 字幕

### 引言 `00:00`

`00:00` git与GITHUB已经成为了AI时代
`00:02` 必学必会的基本功之一了
`00:04` 很多AI agent的核心功能
`00:06` 都是直接围绕着git来运行的
`00:08` 好消息是
`00:09` 在AI时代
`00:10` 我们不再需要死记硬背一大堆git命令
`00:13` 只需要掌握git和GITHUB的几个核心概念
`00:16` 就可以直接用自然语言指挥AI完成各种操作
`00:19` 本期视频带来一个git与GITHUB核心概念大串讲
`00:23` 我会使用动画讲解
`00:25` 结合实战演示的方式
`00:26` 把所有的核心概念一次讲清楚
`00:29` 我们先从从本地git仓库开始介绍提交分支
`00:33` 分区等核心概念
`00:34` 然后引入远端仓库
`00:36` 详细介绍GITHUB网站的基础用法
`00:38` 包括怎么在GITHUB中进行多人协作
`00:41` 相信看完这一期
`00:42` 你就能快速掌握AI时代所需要的
`00:45` git与GITHUB的相关知识
`00:47` 话不多说
`00:47` 我们直接进入第一个章节

### Git与GitHub是什么 `00:50`

`00:50` 我们先把GITHUB这个名字拆开
`00:52` 就是get it加hub两个单词
`00:54` 我们先看左半边的git
`00:56` git是一个开源免费的软件
`00:58` 它的功能是版本控制
`01:00` 各位上学写论文的时候
`01:01` 一定有这样的经历
`01:02` 论文写了第一版
`01:03` 第二版
`01:04` 第三版
`01:05` 定稿版
`01:05` 最终版打死不改
`01:06` 最终版我们小心翼翼地保存好论文的每个版本
`01:10` 就是为了应对各种突发情况
`01:12` 比如某一天
`01:13` 你突然需要找回论文的某个历史版本的
`01:15` 某个段落
`01:16` 我们就可以很容易地从历史文件把它找回来
`01:19` 这个保存了多个历史版本的操作
`01:21` 就是最原始的版本控制
`01:23` 不过这是一种纯人工的方式
`01:25` 想象一下
`01:26` 如果一个项目里面有成千上万的文件
`01:28` 又有成百上千的人对这些文件进行协同开发
`01:31` 版本控制会变成一个极其复杂的工作
`01:34` 纯人工的方式肯定无法负担这些操作
`01:37` 为了解决版本控制的难题
`01:39` get it诞生了
`01:40` 事实上
`01:41` git是世界上最多人使用的版本控制系统
`01:44` 当一个文件夹被git管理起来以后
`01:47` 就变成了一个git仓库
`01:49` 被git仓库管理的文件夹
`01:50` 下面会生成一个点git的子文件夹
`01:53` 用来存放git的版本控制信息
`01:56` git使用commit
`01:57` 也就是提交作为版本控制的基本单元
`02:00` 每完成一次commit
`02:01` git都保存了一个仓库
`02:03` 此时状态的快照文件夹里
`02:05` 所有文件的状态都被记录了下来
`02:08` 随着commit的越来越多
`02:09` 会形成一条commit的历史链路
`02:11` 这样整个仓库都是可回溯
`02:13` 可以查看历史的
`02:15` 每个项目参与者的每一次改动
`02:17` 都会被这个commit链路记录下来
`02:19` git的仓库分成两种
`02:21` 分别是local repository
`02:22` 本地仓库
`02:23` 还有remote repository远端仓库
`02:26` 本地仓库指的是运行在你自己电脑上的git仓库
`02:29` 如果你想把本地仓库在远端服务器上
`02:32` 进行一个备份
`02:33` 或者想跟其他人分享你的代码
`02:35` 就可以把本地仓库上传到服务器上面
`02:38` 这样就拥有了远端仓库
`02:40` 我们可以自己购买服务器
`02:41` 自行搭建远端仓库
`02:43` 当然也可以使用别人搭建好的远端仓库
`02:46` GITHUB就是这样一个免费提供远端仓库的网站
`02:49` 当然除了GITHUB以外
`02:51` 还有其他类型的远端仓库的服务
`02:53` 比如glab beat bucket等等
`02:55` 如果你学会了get up
`02:56` 也可以很轻松地使用另外两个服务
`02:59` 我们还是回到GITHUB这个名字
`03:01` 我们来看右半边
`03:02` hub的意思是中心汇聚集合全世界的
`03:06` 很多开发者会把自己用git进行版本控制的仓库
`03:09` 也就是一个个的项目文件夹上传到GITHUB网站上面
`03:13` 这个网站就变成了git仓库的集合
`03:16` 也就是hub
`03:17` 所以git加hub就是get up
`03:20` 它是全球最大的代码仓库托管与协作平台
`03:23` 可以在里面存储分享
`03:25` 并且与他人一起编写代码
`03:27` GITHUB上面的仓库分成两种
`03:29` 公开仓库
`03:30` 还有私有仓库
`03:31` 所有的公开仓库在GITHUB上面都可以被搜索到
`03:35` 而且这些公开仓库都是开放源代码的
`03:38` 所有人都可以进去直接浏览学习其中的代码
`03:41` 并且根据一定的规则使用这些代码
`03:44` 比如大名鼎鼎的LINUX操作系统
`03:46` Python的C语言解释器
`03:48` C python
`03:49` 还有NGX等等
`03:50` 都是托管在GITHUB上面的开源项目
`03:53` 所以开源是计算机科学的基石与瑰宝

### 实战准备 `03:57`

`03:57` 在开始之前
`03:58` 我们需要在电脑上进行一些准备工作
`04:00` 也就是在电脑上安装git vs code
`04:03` 还有要配置至少一个AAG
`04:06` 我们还需要注册并且登录GITHUB网站
`04:08` 在我之前的视频从零开始
`04:10` 用国内网络跑通一切agent里面有详细的步骤介绍
`04:14` 我们先来安装git
`04:16` 这里我我们先看windows电脑
`04:17` 我们来到git的官网
`04:19` 点击下载windows系统的安装包
`04:21` 然后选择自己的CPU架构
`04:23` windows系统一般是X86614位的下载
`04:26` 并且打开安装程序
`04:28` 一路点击下一步完成安装
`04:30` 我们来看Mac电脑安装git
`04:32` 在应用程序里面找到终端
`04:34` 然后输入这个命令
`04:35` x code横杠select空格
`04:38` 两个横杠install来安装Mac系统的命令行工具包
`04:41` 接下来会弹出一个确认窗口
`04:43` 我们点击同意Mac系统的命令行工具包
`04:46` 包含许多开箱即用的工具
`04:48` 其中就包含git
`04:50` 我们可以输入命令get it两个横杠version
`04:52` 这里成功打印出版本号
`04:54` 就安装完成了
`04:55` 下一步我们来安装vs code
`04:57` 我们来到vs code的官网
`04:59` 这里会根据你的操作系统提供对应的安装包
`05:02` 比如windows系统的
`05:04` 一路点击下一步完成安装
`05:06` 接下来我们注册一个GITHUB账号
`05:08` 如果我们访问GITHUB遇到网络问题
`05:11` 可以参考之前的这期视频
`05:13` 如果没有KEUP账号的话
`05:15` 可以在右上角点击3up普
`05:17` 先注册一个
`05:18` 填写一个用户名
`05:19` 邮箱密码
`05:20` 然后点击注册
`05:21` 接下来我们去邮箱收一下验证码
`05:23` 把验证码填写过来
`05:25` 这样我们就登录进了GITHUB
`05:27` 接下来我们配置一个AI软件
`05:29` 一般使用codex或者cloud code
`05:32` 在之前的这期视频也有过详细介绍
`05:35` 接下来的一个步骤非常重要
`05:37` 就是先让AI帮我们完成git与GITHUB的绑定
`05:40` 这里我以codex为例
`05:41` 我先建立一个空白的文件夹
`05:43` 然后我们进入codex
`05:45` 点击进入项目目录
`05:47` 使用现有文件夹
`05:48` 选择我们刚才新建的文件夹
`05:50` 输入提示词
`05:51` 把这个文件夹初始化成git项目
`05:54` 并且推送到GITHUB上面
`05:56` 这里AI要求我们提供一个远端仓库的地址
`05:59` 我们来到GITHUB网站
`06:01` 点击这个new按钮
`06:02` 也就是在GITHUB上面创建一个repository存储库
`06:06` 然后填写一个存储库的名字
`06:08` 接下来点击创建
`06:09` 然后我们把存储库的地址复制一下
`06:12` 粘贴给codex
`06:13` 接下来弹出了一个授权窗口
`06:15` 我们点击蓝色按钮
`06:17` 先登录get up完成授权
`06:19` 最后任务就完成了
`06:20` 我们就完成了git与GITHUB的绑定准备工作完成以后
`06:24` 我们来看git的第一个概念
`06:26` INIT就是初始化的意思
`06:29` 把一个文件夹进行git init
`06:31` 就是把这个文件夹使用git管理起来
`06:33` 让这个文件夹变成一个git仓库
`06:36` 这是使用git的第一步
`06:37` 在vs code里面选择open folder
`06:40` 打开电脑上的任意一个文件夹
`06:42` 这个文件夹可以是空文件夹
`06:44` 也可以是非空的
`06:45` 找到左侧的s control
`06:47` 也就是源代码管理
`06:48` 点击这个initialize repository按钮
`06:51` 这样就完完成了仓库的初始化
`06:53` 初始化完成以后
`06:54` 我们看到这个文件夹里面多了一个点
`06:56` git的子文件夹
`06:57` 这个子文件夹里面存储了一一切
`07:00` 与git有关的数据
`07:01` 如果把这个点get it子文件夹删除
`07:04` 就取消了git对文件夹的管理
`07:06` 这个文件夹也就不再是一个git仓库了
`07:09` ignore的英文意思是忽略点git
`07:12` ignore是一个文件
`07:14` 这个文件里面声明了
`07:15` 仓库里面哪些文件不应该受到git的管理
`07:18` 比如我们文件夹里面有有存储了密钥的
`07:20` 点EV文件
`07:21` 这些文件应该安全地保存在本地
`07:24` 不应该被git管理
`07:25` 更不能上传到GITHUB上面
`07:27` 我们可以在仓库根目录新建一个点git
`07:30` 一个now文件
`07:31` 然后在里面写一行点ENV
`07:33` 那这个文件就不再被git管理
`07:35` 还有一种情况
`07:36` 像是node js项目的node modules文件夹
`07:39` 存放了所有第三方依赖包
`07:41` 通常里面包含成千上万个文件
`07:44` 如果其他人需要
`07:45` 可以使用npm install随时下载
`07:47` 因此我们通常会把node modules目录也添加进点
`07:51` Git ignore
`07:52` 我们在点get it ignore里面写一行node modules
`07:55` 注意末尾要添加一个斜杠
`07:57` 表示这是一个目录
`07:58` 那这样整个和目录就被排除在外
`08:01` 点git一个now
`08:02` 最好在项目初始化的时候就一起创建好
`08:04` 这样可以从一开始就避免
`08:06` 把不应该提交的内容物提交到仓库里里面
`08:10` 所以我们使用AI对目录进行get it
`08:12` 初始化的时候可以这么说
`08:14` 把这个目录进行get it初始化
`08:16` 注意把不需要的内容都排除掉
`08:18` 我们看到为我们完成了git初始化
`08:21` 并且帮我们创建好了点git ignore文件
`08:23` 它自动把存放了密钥的文件
`08:25` 还有node modules等等都排除出去了
`08:28` commit的英文意思是提交

### Commit提交 `08:29`

`08:30` 比如视频开头写论文的那个例子
`08:33` 每保存一个论文的历史版本就是一次提交
`08:36` 我们每完成一次commit
`08:37` git都保存了仓库此时状态的快照
`08:40` 所有文件的状态都被记录了下来
`08:43` 随着commit越来越多
`08:44` 会形成一条commit的历史链路
`08:47` 这样整个仓库都是可回溯
`08:49` 可以查看历史的
`08:50` 每个参与者的每一次改动
`08:52` 都会被commit链路记录下来
`08:54` 我们来到vs code
`08:55` 进入项目目录
`08:56` 这里我可以创建一个新的文件fruits
`08:59` 点TT
`09:00` 然后我们在里面新加一行相交
`09:02` 接下来我们点击左侧的control按钮
`09:05` 这里就显示了两个等待提交的文件
`09:07` 一个是我们刚才创建的git ignore
`09:10` 还有水果清单
`09:11` 接下来填写一个commit message
`09:13` 也就是提交摘要
`09:14` 主要讲清楚这次提交都做了什么事情
`09:17` 这里我填写一个提交摘要
`09:19` 然后点击commit
`09:20` 这样我们第一次提交就完成了
`09:22` 接下来我再修改一下
`09:23` 又增加了一个水果
`09:24` 还是跟刚才一样
`09:26` 点击commit提交上去
`09:27` 我还可以修改一行再提交上去
`09:31` 如果对某一次代码修改不满意
`09:33` 我们可以找到这个文件
`09:34` 点击这里的discard changes
`09:37` 也就是放弃这次代码修改
`09:39` 把代码回退到上一次提交
`09:40` 也就是更改之前的状态
`09:42` 我们使用AI编程的时候
`09:44` 有一个技巧
`09:45` 就是每次让AI完成一个小的功能点
`09:47` 我们就做一次commit
`09:49` 这样可以非常好的保存好每次的历史记录
`09:52` 如果我们发现AA的更改不符合预期
`09:55` 可以随时点击这个discard按钮
`09:57` 抛弃掉AA的更改
`09:59` 然后我们就可以修改提示词
`10:00` 让AI重做
`10:02` 在下面的graph里面
`10:03` 可以看到每次提交的历史链路
`10:06` 然后展开具体的某一次commit
`10:08` 可以看到那次提交的文件改动情况
`10:10` 如果想要查看某次提交当时的代码情况
`10:14` 我们可以把鼠标移动到某次提交上面
`10:16` 右键点击check out detached
`10:19` 这样整个仓库就回退到了某个历史版本
`10:22` 不过这个状态只适合查看代码
`10:24` 并不适合更改任何代码
`10:26` 原因就在于detach的这个词
`10:28` 中文翻译过来叫做分离头指针
`10:31` 分离头指针表示
`10:32` 当前不处于任何一个分支的管理中
`10:35` 在这里面修改容易搞丢代码
`10:37` 如果想把仓库回到最新的状态
`10:39` 我们可以点击左下角这个按钮
`10:41` 然后选择master
`10:43` 这样我们的仓库又重新回到了主干
`10:45` 也就是最新的状态
`10:47` 每次提交除了有commit message
`10:49` 也就是这里的新增苹果
`10:51` 新增荔枝
`10:52` 这种话
`10:52` 每次提交也都是有id的
`10:54` 我们把鼠标放到某一次提交上面
`10:57` 在这里有一个数字加英文的小编号
`11:00` 这个就是提交的id叫做commit id
`11:02` 这个id是git通过哈希算法自动计算出来的
`11:06` 所以commit id又叫做commit哈希
`11:09` 我们把它来复制一下
`11:10` 粘贴到一个空白文件里面
`11:12` 我们发现粘贴出来它变长了
`11:14` 再来对比一下
`11:15` 其实这个长id
`11:17` 前面七位跟短id长得是一模一样的
`11:19` 所以commit id有两种
`11:21` 一种是长id
`11:22` 一种是短id
`11:23` 短id比较方便记忆
`11:25` 它们两个在功能上是完全一样的
`11:27` commit id在一个仓库里面是绝对唯一的
`11:30` 所以我们跟AI交流的时候
`11:32` 最好直接使用commit id
`11:34` 这样会更加的精确
`11:35` 我们来试一下
`11:36` 这里我想让AI帮我把删除跟苹果这两次提交
`11:40` 整个移除掉
`11:41` 把仓库回退到荔枝这个状态
`11:43` 我们在荔枝上面右键选择copy commit哈
`11:46` 希然后把这个id粘贴给A
`11:49` 你把仓库还原到这次提交的状态
`11:51` 他后面的所有提交都不要了
`11:53` AI在底层执行的核心命令其实是git reset hard
`11:57` 但是在AI时代
`11:58` 我们完全不需要记忆这些命令
`12:00` 你只需要了解commit的基础概念
`12:03` 就能让AI完成一切操作
`12:05` 我们回到仓库看一下
`12:06` 现在整个仓库已经回退到了对应的状态
`12:09` 后面两次提交被删除了
`12:10` 我们再来看一个例子
`12:12` 比如我发现某一次的提交是错误的
`12:14` 就是这个香蕉改菠萝
`12:16` 这一次我想把这一次提交的改动撤回
`12:19` 但是又不想影响其他的提交
`12:21` 这里我们右键还是复制这一次提交的哈
`12:24` 希粘贴给AI
`12:25` 你把这一次的提交撤销掉
`12:27` 保持其他的不变
`12:28` 我们看到AI在底层执行的是git revert命令
`12:32` 生成了一个反向提交
`12:33` 抵消了那次的操作
`12:35` 这样就可以保持其他的提交不变
`12:37` 我们再看一眼仓库
`12:38` 最新的是一次反向的提交
`12:40` 抵消了那次的操作
`12:42` 我们并不需要记忆命令
`12:43` 只需要用自然语言就可以让AI完成操作
`12:46` 刚才我们介绍了三种get it后悔药
`12:49` 这里简单总结一下
`12:50` 首先是discard
`12:51` discard的行为是放弃掉还没有commit的更改
`12:55` 它的使用场景是当文件更改还没有commit
`12:58` 这时候就可以使用discard来撤销掉
`13:01` 第二个是reset
`13:02` reset的行为是把仓库强制回退到某个历史状态
`13:06` 在单人分支上或者被撤销的改动
`13:08` 还没有提交到远端仓库的时候
`13:10` 可以使用reset
`13:11` 如果被撤销的改动已经上传过远端仓库
`13:15` reset以后
`13:15` 就必须使用强制推送来强制覆盖远端仓库
`13:19` 这种操作有搞丢代码的风险
`13:21` 因此在多人协作的分支上是禁止使用reset的
`13:25` 第三个是revert
`13:26` revert是通过生成一个反向的提交
`13:29` 来抵消掉某次commit
`13:30` 所以revert比较安全
`13:32` 在多人协作的分支上
`13:34` 应该优先使用revert来回退
`13:36` 更改好
`13:36` 我们再看一个例子
`13:38` 我想比较一下新增荔枝跟新增橘子
`13:41` 这两次提交之间修改了什么内容
`13:43` 我还是把两次的commit id复制下来
`13:46` 我让AI比较一下这两次提交有什么区别
`13:49` 修改了什么内容
`13:50` AI帮我们完成了统计
`13:51` 列出了几次提交的改动
`13:53` 修改的文件统计的非常好
`13:55` 我们看到AI在底层执行的是git def命令
`13:59` 不过我们完全不需要记忆这些命令
`14:01` 只需要理解commit的概念就可以完成操作
`14:04` 说到这里
`14:05` 我想起了庄子里面的一句话
`14:07` 言者所以在意得意而忘言
`14:09` 也就是在AI时代
`14:11` 我们只需要理解git操作的本意与原理即可
`14:14` 对具体的命令并不需要死记硬背
`14:17` branch的英文意思是分支分支

### Branch分支 `14:18`

`14:20` 简单来说就是存储库的不同开发线
`14:22` 默认情况下
`14:23` 每个仓库都有一个main分支或者master分支
`14:26` 也就是主干分支
`14:28` 创建分支
`14:29` 也就是创建一个副本
`14:30` 这里补充一下
`14:31` 创建分支的操作
`14:32` 在git里是通过指针来实现的
`14:34` 并不需要拷贝代码
`14:36` 比如我在main分支上创建一个feature
`14:38` 分支
`14:39` feature刚创建出来的时候
`14:40` 它与主干的代码是一样的
`14:42` 当你对feature进行代码修改
`14:44` 并且完成commit
`14:46` 这些新的提交只对feature分支有效
`14:49` 只能在feature被看到
`14:50` 而不会影响主干
`14:51` 换句话说
`14:52` 当你切换回men主干的时候
`14:54` 是看不到feature分支的代码修改的
`14:57` 这就意味着
`14:58` 在各自分支上的代码修改不会互相影响
`15:01` 分支对于多人或者多A阵的协作非常有用
`15:04` 每个人或者每个agent都可以创建出自己的分支
`15:08` 在分支上进行功能开发
`15:10` 而不会影响到其他人
`15:12` 一旦功能开发结束
`15:13` 就可以把分支合并回主干
`15:15` 这个操作称为merge
`15:17` 也就是合并
`15:18` 我们来到vs code左下角显示我们现在master分支
`15:22` 点击一下这个按钮
`15:23` 然后点击create new branch
`15:25` 就是基于master分支创建一个新的分支
`15:28` 这里分支名我叫做feature
`15:29` 左下角这里改成了feature
`15:31` 表示我们当前处于feature分支
`15:33` 然后我又改了一行代码提交上去了
`15:36` 我们可以点击左下角这个分支的按钮
`15:38` 在分支之间来回切换
`15:40` 比如这里我切换回master
`15:42` 我们刚才那个新增苹果的提交在上是看不到的
`15:46` 如果想让那次提交出现在master上
`15:49` 我们需要把feature分支合并进master
`15:51` 这里我可以跟AI说
`15:52` 把feature合并进master
`15:58` 新增苹果这次修改也从feature分支合并进来了
`16:01` 当我们在一个分支上开发完毕
`16:03` 并且把代码合并了以后
`16:05` 一般来说我们就会删除掉这个分支
`16:08` git不允许我们删除当前分支
`16:10` 所以删除分支的时候
`16:12` 我们先把分支切换走
`16:13` 这里先切换回master
`16:15` 然后点击右键delete feature
`16:17` 这样就把feature分支删除了
`16:19` 我们点击按钮
`16:21` 基于master创建的分支
`16:22` 是基于master的最新次提交创建的
`16:25` 我们也可以基于任何一次历史提交创建分支
`16:28` 比如这里找到芒果
`16:29` 这次提交
`16:30` 点击create branch
`16:31` 我基于芒果的这个状态创建了feature two分支
`16:35` 仓库的文件
`16:36` 也还原到了那次提交对应的状态上面
`16:38` 这样我们们介绍了两种
`16:40` 把仓库回退到历史的方法
`16:42` 第一种右键check out detached
`16:44` 也就是分离头指针
`16:46` 在分离头指针的状态下
`16:48` 最好不要进行修改
`16:49` 容易搞丢代码
`16:50` 第二种方法就是基于某个历史提交
`16:53` 直接创建分支
`16:54` 这样代码有了分支的管理就可以自由地修改
`16:57` 不容易搞丢了
`16:58` 除了基于main创建分支
`17:00` 我们也可以基于任何一个分支来创建分支
`17:02` 比如我们可以基于feature分支
`17:04` 创建一个feature2分支
`17:06` feature2会包含之前feature跟main上面所有的代码
`17:10` 这时候如果我们基于main创建feature3分支
`17:13` 那feature3分支只能看到main分支的代码
`17:16` 我们再来介绍一个git的概念
`17:18` 也就是head的head的一句话总结
`17:21` 就是当前仓库处于哪一次commit上
`17:23` 比如我现在切换到main分支
`17:25` 当前仓库展现的是men分支
`17:28` 最新的一次提交
`17:29` head就指向commit3上面如果切换到feature3分支
`17:33` head指向commit7
`17:35` 如果切换到feature分支
`17:36` head指向commit5
`17:38` 还有一种情况就是head的
`17:40` 它不指向任何一个分支
`17:42` 而指向某一次历史提交
`17:43` 就是我们视频前面演示的check out detach的状态
`17:47` 也就是分离头指针的状态

### WorkTree `17:50`

`17:50` work tree也就是git工作数
`17:52` 它本质上是用git创建一个新的分支
`17:55` 然后把这个新分支的代码完整地复制一份
`17:58` 到一个新的文件夹里面
`18:00` 这个新文件夹就是一个work tree
`18:02` 主文件夹和分支文件夹可以并行的工作
`18:05` 我们可以在两个文件夹里面各自修改代码
`18:08` 互相不干扰
`18:09` 我们也可以基于主干创建多个分支
`18:12` 他们在底层是通过git关联在一起的
`18:15` 分支文件夹的改动都能轻松合并回主干
`18:18` 我们在codex上找到我们的项目
`18:21` 点击右键创建永久工作数
`18:23` 然后给分支起个名字
`18:25` 点击创建
`18:26` 我们看到codex把新创建的工作数
`18:29` 放到了一个全新的文件夹下面
`18:31` 我们可以在这个文件夹下面进行开发测试
`18:34` 做任何的操作都不会影响到主干文件夹
`18:37` 所以它可以跟主干文件夹并行进行开发工作
`18:40` 我们在工作数上完成了开发以后
`18:43` 就可以让AI把工作数合并回主干
`18:45` 在我之前的这期codex教程里面
`18:48` 有这块的详细实战
`18:49` 接下来我们看一下cloud code怎么使用work tree
`18:52` 这里我们来到项目文件夹
`18:54` 右键在终端打开
`18:55` 然后输入这个命令
`18:56` cloud两个横杠
`18:58` Work tree
`18:58` 接下来填写分支的名字
`19:00` 这里我叫ADD coconut
`19:02` 这样启动的cloud code就处于了这个work tree里面
`19:05` work tree的目录就是它这里显示出来的目录
`19:08` 然后我让cloud code干点活
`19:10` 给水果清单添加椰子
`19:11` 我们来到work tree的目录
`19:13` 可以看到水果清单里面添加了椰子
`19:15` 但是在主干的这个主文件夹里面是没有的
`19:18` 接下来我让cloud code提交
`19:20` 然后合并回主干
`19:21` 最后删除掉当前的work tree
`19:23` 我们看到它通过git默认声命令领完成了合并
`19:26` 然后把work tree删除掉了
`19:28` 在主干分支上
`19:29` 这次提交也合并进来了
`19:31` 我们可以使用这个命令
`19:32` 在不同的窗口里面创建多个work tree
`19:35` 让它并行工作
`19:36` 工作完成以后再合并回主干
`19:38` 这样可以提高工作效率
`19:40` 如果两个分支修改了同一个文件的同一行代码
`19:44` 合并的时候
`19:45` git无法自动确认到底保留哪个改动
`19:48` 这个场景就是merge conflict
`19:50` 也就是合并冲突
`19:52` 这个时候我们需要人工决定
`19:54` 到底保留哪个分支的代码
`19:55` 这个过程就是解决冲突
`19:57` 解决冲突在git里面其实是个比较繁琐的操作
`20:01` 不过现在有了AI了
`20:02` 一切就简单太多了
`20:04` 我们来看一个例子
`20:05` 这里我告诉codex创建一个新的分支
`20:07` 把第二行改成西瓜
`20:09` 再创建一个新的分支
`20:10` 把第二行改成草莓
`20:12` codex完成了操作
`20:13` 我们回仓库看一
`20:14` 当我们在草莓分支的时候
`20:16` 第二行是草莓
`20:17` 当我们切换成西瓜分支
`20:19` 第二行就是西瓜
`20:20` 这样两个分支改动了同一个文件的同一行
`20:23` 它们合并就会产生冲突
`20:25` 我让codex把两个分支都合并进主干
`20:27` 然后遇到冲突就停下来
`20:29` 让我选择怎么处理
`20:30` 这里合并的时候出现了冲突
`20:32` codex询问我应该怎么保留
`20:34` 他给了我几个选项
`20:35` 我们可以选择保留任意一个
`20:37` 也可以选择两个都保留
`20:39` 这里我选择两个都保留
`20:40` 顺序是西瓜在前
`20:42` 草莓在后
`20:42` 这样codex就按照我的要求解决了冲突
`20:45` 他保留了两个水果
`20:47` 最后冲突解决了
`20:48` 合并也成功了
`20:49` 我们来介绍一组git的概念

### Git分区 `20:52`

`20:52` 也就是分区
`20:53` 第一个分区是working directory
`20:55` 也就是工作区
`20:56` 工作区就是你电脑磁盘上的本地文件夹
`20:59` 下一个分区是local repository
`21:01` 也就是本地仓库commit以后
`21:03` 你的文件改动就提交到了本地仓库下一个分区
`21:07` Remote repository
`21:08` 也就是远端仓库
`21:09` 一般来说我们指的就是GITHUB网站
`21:12` 当然还有其他类型的远端仓库
`21:14` 比如glab get ee等等
`21:16` 远端仓库用来分享或者备份代码
`21:19` 只有推送到远端仓库的代码才能被其他人看到
`21:22` 大部分git操作的本质
`21:24` 就是从这些分区里面互相同步文件
`21:26` 首先git clone是把远端仓库保存到本地
`21:30` 同时创建出本地存储库跟本地工作目录
`21:33` 使用git ADD加git commit命令
`21:36` 可以把本地工作区的改动提交到本地仓库
`21:39` 当你准备与其他人分享你的进度
`21:42` 或者想把代码做一个远程备份
`21:44` 我们可以使用git push
`21:45` 将本地仓库的改动推送到远端存储库
`21:49` 在git中协作是一个双向的代码交换
`21:52` 我们可以使用git pu
`21:53` 将远端存储库的最新改动更新
`21:56` 并且合并到本地
`21:57` git put命令同时解结合了两个命令
`21:59` 一个是git fetch
`22:01` 另外一个是git merge
`22:02` 用来将远端的最新改动
`22:04` 合并进你的本地工作区
`22:06` 所以也可以使用git fetch加git merge
`22:09` 分两步来同步远端代码
`22:11` git还有第四个分区就是staging area
`22:14` 或者叫做index area
`22:15` 中文翻译过来是暂存区
`22:17` 可以把暂存区看作是commit之前的一个检查点
`22:21` 我们可以在暂存区里面完成commit
`22:23` 提交前的准备工作
`22:25` 一般来说我们提交之前需要先使用git a的命令
`22:28` 把文件保存到暂存
`22:30` 然后使用git commit命令
`22:32` 把暂存区的所有改动提交到本地仓库
`22:35` 不过为了方便操作
`22:36` 现阶段的大部分git工具都淡化
`22:39` 或者说隐藏了暂存区的概念
`22:41` 比如在vs code里面
`22:43` 我们点击这个按钮执行commit动作的时候
`22:45` 同时也执行了git i的命令
`22:48` 这俩命令在vs code里面合并成了这一个按钮
`22:51` 这样大幅简化了git的操作复杂度
`22:53` 这里我再补充一下
`22:55` 在vs code里面也是可以使用暂存区的
`22:58` 比如现在我修改了四个文件
`23:00` 我们可以在某个文件的右侧点击这个加号
`23:03` 这里写的是stage changes
`23:04` 也就是把这个文件通过git i的命令
`23:07` 添加到暂存区里面
`23:08` 这里我先添加两个
`23:10` 这里写了stage的changes
`23:12` 也就是这两个文件在暂存区里面
`23:14` 接下来我们在提交的时候
`23:16` 就只会提交暂存区里面两个文件
`23:18` 如果我们不选择把哪个文件添加到暂存区
`23:21` Vs code
`23:22` 默认就是把所有的文件一起全部commit
`23:25` 我们来实战一下远端仓库的操作

### 远端仓库实战 `23:26`

`23:28` 本地仓库有两种绑定远端仓库的方法
`23:31` 第一个是git clone
`23:32` 把一个远端仓库克隆到本地
`23:34` 第二个是git push
`23:36` 把本地仓库上传到远端
`23:37` 我们先看第一种方法
`23:39` Git clone
`23:40` 我们来到GITHUB首页
`23:41` 点击这个new按钮
`23:42` 在GITHUB上面创建一个新的远端仓库
`23:45` 这里给仓库起一个名字
`23:47` 在下面可以选择是公开仓库
`23:49` 还有私有仓库
`23:50` 如果是公开仓库
`23:51` 你仓库里面的代码就会公布在网上
`23:54` 所有人都可以查看里面的代码
`23:56` 如果选择私有仓库
`23:57` 则只有你自己可以看到
`23:59` 这里我选择公开
`24:00` 在下面还可以选择则是否添加read me文件
`24:03` read me文件就是这个仓库的说明文件
`24:06` 是否同步创建git ignor
`24:08` 是否使用开源许可证
`24:09` 这里我直接创建好
`24:11` 这样我们的仓库就创建好了
`24:13` 这里给到一个链接
`24:14` 就是仓库地址
`24:15` 我们看到这个地址
`24:16` 中间这一串是我们在GITHUB上面的英文名字
`24:19` 后面这一串是仓库的名字
`24:21` 然后我们把这个地址复制一下
`24:23` 然后我们回到电脑上
`24:24` 还是打开vs code
`24:26` 这里在菜单里面点击close folder
`24:28` 先退出
`24:29` 当前的项目
`24:30` 然后在source control这里有一个克隆按钮
`24:33` 我们点击一下
`24:34` 填写上刚才项目的地址回车
`24:36` 接下来他让我们选择一个文件夹
`24:38` 就是把这个项目
`24:39` 克隆到你本地的哪个文件夹里面
`24:41` 这里我选择桌面
`24:43` 选择确认
`24:43` 桌面上就多了这么一个文件夹
`24:45` 然后我们就可以在项目里面新增文件进行commit
`24:49` 再点击publish按钮
`24:51` 把文件提交到GITHUB上面
`24:53` 我们回到GITHUB刷新一下
`24:55` 就看到了我们在本地提交上来的文件
`24:57` 这样我们就使用了git clone
`24:59` 把一个远端仓库克隆到本地
`25:01` 同时创建出本地仓库跟本地工作目录
`25:04` 接下来我们修改代码
`25:05` 通过commit提交到本地仓库
`25:08` 然后再在本地仓库通过publish提交到远端仓库
`25:12` 这里有个注意事项
`25:13` 我们使用get it push的时候
`25:15` 只有你是仓库管理员或者管理员
`25:17` 给你添加了权限
`25:18` 你才可以push成功
`25:20` 你是不能往别人的仓库里面push代码的
`25:23` 如果想参与贡献开源项目
`25:25` 在后面的章节
`25:26` 我们会介绍怎么在别人的仓库里面参与贡献
`25:29` 接下来我们看绑定远端仓库的第二种方法
`25:32` 就是先初始化一个本地仓库
`25:34` 然后再推送到远端仓库这里
`25:37` 我有一个测试文件夹里面已经有一些代码了
`25:39` 但是他还没有点git文件夹
`25:42` 也就是说它还不是一个git仓库
`25:44` 我们来到vs code这边
`25:45` 先打开这个文件夹
`25:47` 然后点击resource control
`25:48` 点击INIT按钮
`25:49` 先把它初始化成一个git仓库
`25:52` 接下来我们做一次提交
`25:53` 仓库里面的文件先commit上去
`25:55` 然后我们点击这个public按钮
`25:57` 填写一个远端仓库的名字
`25:59` 选择则是公开还是私有
`26:01` 这里我选择私有仓库好
`26:02` 这样就推送成功了
`26:04` 我们点击右下角的open
`26:05` 这样在GITHUB上面就可以看到我们的代码
`26:08` 这里写着private表示他是一个私有仓库
`26:11` 只有我自己才可以看到其他人访问这个地址
`26:13` 是看不到这些代码的
`26:15` 接下来我们看怎么把远端仓库的改动
`26:17` 同步到本地
`26:18` 这里
`26:18` 我直接在GITHUB上面
`26:19` 也就是远端仓库上面进行一点更改
`26:22` 比如这里点击ADD file
`26:24` 点击create new file
`26:25` 创建一个新的文件
`26:26` 这样我创建了一个新的文件
`26:28` 写了一行
`26:28` 点击右上角的commit
`26:30` 点击确认
`26:31` 这个commit我是直接在远端仓库进行的更改
`26:34` 所以在本地暂时还看不到
`26:36` 我们在source control这里点击这里的s y NC changes
`26:40` 这有一个向下的箭头
`26:41` 表示一也就是远端有一个提交
`26:43` 我们点击这个按钮
`26:45` OK这样就把远端的提交通过git库拉到了本地
`26:48` 我们看到这个文件也过来了
`26:49` 除了直接在远端仓库修改代码
`26:52` 还有一种更常见的情况是
`26:54` 同事或者协作者
`26:55` 把他自己的提交先上传到了远端仓库
`26:58` 这时候一般我们先使用git铺
`26:59` 先把远端仓库的改动拉取到本地
`27:02` 有了远端仓库以后
`27:03` 我们就有了两种分支
`27:04` 我们看到这里写的是origin main
`27:07` origin的意思就是远端仓库
`27:09` 然后这里的main表示我的本地仓库
`27:11` 在这个图上可以看到我的本地仓库的main分支
`27:14` 比起远端仓库
`27:15` 现在多了两个提交
`27:17` 上面的按钮变成了s y NC changes to
`27:19` 一个上箭头表示我本地有两个提交
`27:22` 还没有上传到远端仓库
`27:24` 我们点击一下这个按钮
`27:25` 这里看到origin main跟men都处于同一个提交
`27:28` 也就是远端仓库跟本地仓库现在是同步的状态
`27:32` 我们来看GITHUB最核心的页面

### GitHub入门 `27:33`

`27:34` repository也叫存储库
`27:36` 代码仓库
`27:37` 首先我们先看到他的网址
`27:39` 这个网址分成了三个部分
`27:44` 中间的doc mirror是开发者的名字
`27:47` 最后的dv side car则是仓库的英文名字
`27:50` GITHUB所有仓库的地址都是遵循这样的命名规则
`27:53` 我们进入这个仓库以后
`27:55` 界面中最显眼的部分是代码库
`27:57` 也就是中间这一部分
`27:58` 里面存放的是整个项目的源代码
`28:01` 我们可以直接点开查看里面的内容
`28:04` 也可以点击右上角的扣的按钮
`28:06` 选择download
`28:07` 把代码作为压缩包
`28:09` 整个的下载下来
`28:10` 当然我们也可以使用这个地址
`28:11` 使用git克隆
`28:13` 把代码拷贝到本地
`28:14` 代码库的每个文件后面都有两条信息
`28:17` 第一个是commit message
`28:18` 第二条则是最后一次的更新日期
`28:21` 如果一个项目中的所有文件
`28:23` 更新日期都是很多年前
`28:24` 那就代表这个项目可能很久没人维护了
`28:27` 代码库的
`28:28` 下面是项目的介绍
`28:29` GITHUB会自动读取read me文件
`28:31` 并把其中的内容展示到这里
`28:34` read me文件一般展示了项目的基础信息
`28:36` 介绍了项目是做什么用的
`28:38` 解决了什么问题
`28:40` 以及如何运行使用
`28:41` 在仓库的右侧还有一个releases模块
`28:44` 这里面记录了项目发布版本的信息
`28:47` 找到这里的ISSETS里面
`28:49` 一般有作者打包好的软件
`28:51` 下载下来就可以直接启动
`28:52` releases中包含项目的版本号
`28:55` 还包含这个版本所有的更新内容
`28:57` 我们按照需要选择不同的版本进行下载
`29:00` releases的上面是about模块
`29:03` 这是项目的简介标签
`29:05` 死start树开源协议等等
`29:07` 其中死star类似视频网站的点赞加收藏的操作
`29:10` 能够反映项目的热度
`29:12` 如果你对一个项目感兴趣
`29:14` 不妨给作者点一个star
`29:15` star的左边是folk
`29:17` 也就是复刻
`29:18` 指的是把项目保存一份到自己的名下
`29:21` 这里点击create fork
`29:23` 可以看到这里产生了一个一模一样的仓库
`29:25` 到了自己的名下路径中间
`29:28` 这里的作者名字换成了我自己
`29:30` tag shp进入巴巴虾folk
`29:32` 可以帮助我们更深入地学习源代码
`29:34` 可以根据自己的需求对源代码进行DIY
`29:37` 还可以尝试把自己的修改通过pull request
`29:40` 也就是合并请求合并进作者的主代码库
`29:43` 为开源项目添砖加瓦
`29:45` 在folk按钮的上面是get up的搜索功能
`29:48` 你可以在这里对项目进行搜索
`29:50` 也可以把原本的内容删除
`29:52` 搜索整个GITHUB上面的内容跟资源仓库的左上角
`29:56` 还有一个重要功能
`29:57` Issues
`29:58` 这里可以与项目作者进行讨论
`30:00` 我们可以点击这里的new医术发起一个讨论
`30:03` 比如提出你在项目中遇到的bug
`30:05` 帮助对项目进行完善
`30:07` 当然也可以提出对项目的新功能的期待
`30:09` 你也可以参与讨论
`30:11` 或者帮助其他人解决问题
`30:13` 医术分成两种
`30:14` open跟close
`30:15` 其中open一般指的是还没有解决的bug
`30:18` 或者还在讨论的问题
`30:20` close的则是指的已经解决的bug
`30:22` 或者已经结束的讨论
`30:24` 我们在使用或学习开源项目的时候
`30:26` 不妨在这里搜索一番
`30:28` 因为有些问题可能别人也遇到过
`30:30` 并且在医术里面已经有人给出了解决方案
`30:33` 我来介绍几个get up好用的快捷键
`30:36` 首先是斜线
`30:37` 它会快速的打开GITHUB的搜索功能
`30:40` 然后是键盘上的T键
`30:42` 它会快速定位到文件搜索栏
`30:44` 我们可以在这里根据文件名字搜索对应的文件
`30:48` 我们搜索一个文件进来以后
`30:50` 就可以查看文件内容
`30:51` 这里还有一个好用的快捷键
`30:53` 是键盘上的L键
`30:55` 我们可以快速定位到一个行号
`30:57` 比如这里我定位到33行
`30:59` 点击go
`31:00` 我们可以选中这个行号
`31:02` 在前面又有很多的功能
`31:04` copy line指的是复制这一行的代码
`31:07` copy primer link是复制一个永久链接
`31:09` 我们可以通过分享这个链接与其他人分享代码
`31:13` 还有一个重要功能是git blame
`31:16` 这个页面可以看出文件的提交历史
`31:18` 也就是每行代码是由谁提交上来的
`31:21` 下一个快捷键我们输入键盘上的问号
`31:24` 这里会打开一个快捷键的快速速查表
`31:27` 快捷键我们可以通过这个表单直接找到
`31:29` 比如这里的GC就是go to code
`31:32` 也就是快速查看代码
`31:33` GI是快速查看医术
`31:35` 我们来试一下
`31:36` 先点G再点I就快速进入了医术页面
`31:39` 如果忘记了某个快捷键
`31:41` 我们要记得使用问号快速查看这个表格
`31:44` 最后一个快捷键是键盘上的句号
`31:47` GITHUB会为我们打开一个网页版的vs code
`31:50` 我们可以在这里面查看代码
`31:52` 搜索像vs code一样安装插件
`31:55` 完成一些简单的代码查看
`31:57` 如果你想对这里面的代码进行调试
`32:00` GITHUB同样提供了一个code space的运行环境
`32:03` 我们点击这个运行跟调试按钮
`32:05` 点击继续工作
`32:06` 点击create new code space
`32:08` 这里我们可以根据需求从GITHUB
`32:11` 申请一个远程的运行环境
`32:13` 这里我选择这个双核8GB内存的运行环境
`32:16` 我们点击运行
`32:17` 这样我们就申请到了一个远程的运行环境
`32:19` 可以直接在浏览器里面调试修改改代码
`32:22` 我们找到右上角的这个run按钮
`32:24` 可以直接调试派森代码
`32:26` 可以看到这里成功地出现了输出
`32:28` 接下来我们看怎么在GITHUB上面进行多人协作

### GitHub多人协作 `32:29`

`32:32` 或者说怎么给开源项目贡献自己的代码
`32:35` 这里我换了一个账号登录GITHUB进行演示
`32:38` 这个账号是爬爬虾的小跟班
`32:40` 比如有一天
`32:41` 爬爬虾的小跟班
`32:42` 在GITHUB上面发现了一个非常棒的开源项目
`32:45` 就是这个技术爬爬虾的开源项目
`32:47` 然后小跟班想为这个开源项目添砖加瓦
`32:50` 贡献自己的代码
`32:51` 这里小跟班是没有权限
`32:53` 直接向这个项目本身提交代码的
`32:56` 所以他要做的第一步
`32:57` 就是点击这里的folk按钮
`32:59` folk的意思是复刻
`33:00` 就是把这个项目复制一份到自己的名字下面
`33:03` 这里小跟班点击fork按钮
`33:05` 然后直接点击create for
`33:07` 这样这个开源项目就复刻了一份
`33:09` 到小跟班自己的名字下面了
`33:11` 接下来小跟班可以在code这里复制项目地址
`33:14` 然后小跟班来到vs code
`33:16` 点击这个克隆按钮
`33:17` 然后把他自己名下
`33:19` 也就是复刻出来的这个子项目的地址填写过来
`33:22` 这样小跟班在本地就打开了这个项目
`33:24` 小跟班在动手修改任何代码之前
`33:27` 一定要先创建分支
`33:29` 不要直接在主干分支上进行修改
`33:31` 创建分支最大的好处是
`33:33` 当小跟班从母项目
`33:35` 或者说从上游项目同步代码的时候
`33:37` 因为小跟班的改动都在分支上
`33:40` 这时候同步代码不会产生冲突
`33:42` 点击左下角的main这里
`33:44` 这里点击创建分支
`33:45` 在这个分支上面新增了一个水果
`33:47` 然后小跟班把这个改动提交上去
`33:50` 再点击publish按钮推送到GITHUB上面
`33:53` 我们在GITHUB上面切换到对应的分支
`33:55` 就可以看到这次提交了
`33:57` 下一步小跟班可以通过创建pull request的形式
`34:01` 把自己的改动合并回上游项目
`34:03` 或者叫做母项目
`34:05` pull request直译过来是拉取请求
`34:07` 不过我更喜欢他的另外一个翻译
`34:09` 那就是合并请求
`34:11` 合并请求
`34:12` 是把一个分支的改动合并进另一个分支的提案
`34:15` 通常来说
`34:16` 项目贡献者先把开源项目复刻一份
`34:19` 到自己的名零下
`34:20` 然后创建分支
`34:21` 在分支上修改代码
`34:23` 并且完成commit
`34:24` 接下来贡献者创建pull request
`34:26` 也就是把自己的分支合并进主干分支的提案
`34:30` por request会展示两个分支之间的代码差
`34:33` 然后开源仓库的管理员会来审核
`34:36` 这个代码的改动
`34:37` 这个过程叫做code review
`34:39` 也就是代码审核
`34:40` 在代码审核过程中
`34:42` 管理员可以提出自己的的修改意见
`34:44` 当管理员确认代码是OK的以后
`34:46` 他就可以同意合并
`34:48` 这样贡献者的代码就会合并进主干
`34:51` 协作开发也就完成了
`34:52` 这里我来演示一个场景
`34:54` 我现在的角色是爬爬虾
`34:56` 在小跟班那边修改代码的同时
`34:58` 上游项目也就是爬爬虾这边也在修改代码
`35:02` 爬爬虾也在水果列表里面新增了一个水果
`35:05` 并且爬爬虾已经提交上去了
`35:07` 小跟班在进行poor request之前
`35:09` 他有一个准备工作必须要做
`35:11` 就是先把母项目
`35:12` 也就是上游项目那边的代码同步过来
`35:15` 确保自己这边已经包含了某项目那边
`35:18` 最新的代码
`35:19` 并且没有冲突
`35:20` 这时候才能创建po request
`35:22` 我们来看一下这个图
`35:23` 如果feature分支跟主干同时提交了代码修改
`35:27` 这时候创建pr
`35:28` 很可能会因为修改了同一个文件而产生冲突
`35:31` 所以正确的做法是
`35:32` 先把主干的改动同步到feature分支上
`35:35` 在本地解决冲突
`35:37` 然后再提交pr
`35:38` 这样才不会有问题
`35:40` 这个操作我们可以交给AI来完成
`35:42` 这里我用小跟班打开codex
`35:44` 注意先把分支切换成feature分支
`35:47` 我把这个链接粘过来
`35:48` 我告诉他这是爬爬虾那边的母项目
`35:51` 我要你把某项目的最新改动先合并进当前分支
`35:54` 注意如果有冲突
`35:56` 让我选择怎么处理
`35:57` 我们看到codex先帮我们从某项目那边同步代码
`36:00` 然后他合并某项目的时候遇到了冲突
`36:03` 这里他问我怎么处理
`36:04` 我告诉他两个都保留
`36:05` 那就先樱桃再李子
`36:07` 这样子项目已经同步了母项目的最新改动
`36:10` 接下来我们记得把本地的改动推送到GITHUB上面
`36:13` 然后我们就可以正式的来创建pr了
`36:16` 这里我们来到小跟的子项目
`36:18` 点击pro request
`36:19` 点击new request
`36:21` 这里一定要选择好合并方向
`36:23` 我们是把下游项目
`36:24` 也就是子项目的feature分支
`36:27` 往母项目那边的梦分支合并
`36:29` 注意好这个箭头的合并方向
`36:31` 点击create prequest
`36:32` 这里可以填写一个标题
`36:34` 比如我填写为水果清单增加更多水果
`36:37` 最后点击pro request
`36:39` 把这个合并请求创建出来
`36:41` 我现在的角色是爬爬虾
`36:42` 也就是这个母项目的管理员
`36:44` 当有人提交pull request的时候
`36:46` 开源仓库的维护者都会收到邮件
`36:49` 然后他在request这里就可以看到所有的合并请求
`36:53` 点击进来就可以查阅这个请求
`36:55` 可以在file changes这里查看对方提交的代码
`36:58` 如果管理员觉得这些代码还需要修改
`37:01` 他可以在下面提交自己的建议
`37:03` 如果他觉得一切没问题
`37:05` 就可以点击这个merge按钮
`37:06` 同意这一次的合并请求
`37:08` 再点击CONFIR
`37:09` 这样就把小助理贡献的代码合并进了主干
`37:12` 在母项目的水果清单里面
`37:14` 就增加了这个樱桃
`37:15` 在右下角的贡献者
`37:17` 这里出现了小助理的名字
`37:19` 这样一次开源项目合作的全流程就完成了
`37:22` 刚才我们介绍的是开源项目的多人协作
`37:26` 还有一种情况
`37:27` 就是开发者跟管理员在同一个公司里面
`37:30` 或者管理员把开发者添加成了协作者
`37:33` 这时候有一个步骤就可以省略了
`37:35` 就是不需要fork代码了
`37:36` 现在我的身份是技术爬爬虾
`37:39` 也就是项目的管理员
`37:40` 管理员可以来到set这里有一个collaborators
`37:44` 也就是项目合作者
`37:45` 这里点击ADD people
`37:46` 然后搜索到小跟班的名字
`37:49` 点击ADD
`37:49` 然后在小跟班的GITHUB账号的首页
`37:52` 右上角有一个收件箱
`37:54` 这里面是一个协作者的邀请
`37:56` 小跟班可以在这里点击确认
`37:58` 这样小跟班就变成了项目的协作者
`38:00` 他就有了权限
`38:01` 直接往这个项目的远端仓库来push代码
`38:04` 这时候小跟班就不需要复刻项目了
`38:06` 他可以直接拉分支
`38:08` 然后提交代码
`38:09` 并且push到远端仓库
`38:11` 在提交pr之前
`38:12` 先合并一下主干上的最新改动
`38:14` 然后提交分支到主干的pull request
`38:17` 接下来管理员审核代码
`38:19` 最后合并cherry pick
`38:21` 字面意思是挑选樱桃
`38:23` 在git的语境里面就是拣选提交
`38:25` 比如我在feature分支上进行了三次提交
`38:28` 分别创建了蔬菜清单
`38:30` 肉类清单跟主食清单三个文件
`38:33` 正常来说
`38:34` 如果把feature分支合并进主干
`38:36` 那这三次commit还有三个新文件都会加入主干
`38:40` 现在我想做的是
`38:44` 而肉类这次提交不合并
`38:46` 这里就可以使用cherry pick来实现
`38:48` 这里我们来把需要的两次提交的id来复制一下
`38:52` 首先是蔬菜这次提交的id复制一下
`38:55` 粘贴给AI
`38:56` 接下来主食这个id复制一下
`38:58` 粘贴给AI
`38:59` 然后我输入提示词
`39:00` 你把这两次提交cherry pick进主干
`39:02` 好AI帮我们进来了
`39:04` 这里我们在焖分支
`39:06` 可以看到蔬菜跟主食这两次提交
`39:08` 而肉类那个提交没有合并过来
`39:10` 这样cherry pick就成功了
`39:12` stash的意思是临时存储
`39:15` 注意这个跟暂存区不是一回事
`39:17` 比如我现在在feature分支上写代码
`39:19` 我想新增一个水果
`39:21` 但是我代码写了一半还没有写完
`39:23` 现在我需要紧急切换到main分支
`39:25` 处理一个临时的任务
`39:27` 如果我在这里直接切换分
`39:28` 那这里写了一半的代码会被直接带过去
`39:31` 这显然不是我想要的
`39:33` 这里有两种解决方案
`39:34` 第一个方案是我直接在feature分支上做一个commit
`39:37` 把代码提交了
`39:38` 然后我再切换到main
`39:40` 第二个方案
`39:40` 我们就使用STH把没写完的代码十存储一下
`39:44` 等下切换回来的时候
`39:46` 再把代码拿出来继续写
`39:47` 这里我来使用一下STH
`39:49` 我们点击这三个点
`39:50` 这里有一个STH
`39:51` 然后点击这个stash
`39:53` 随便起个名字
`39:54` 这里直接回车就行了
`39:55` 这样我们没写完的代码就被存储起来了
`39:58` 然后我们就可以切换回main分支
`40:00` 在焖分支上进行工作
`40:01` 等我们完成了焖分支的工作
`40:03` 我们可以再切换回非常分支
`40:06` 这里需要把没写完的代码拿出来继续写
`40:08` 选择这个pop就是拿回来继续写
`40:10` 这样就把没写完的代码又拿回来了
`40:13` REBASE的意思是变机
`40:15` 这个属于git的进阶操作了
`40:17` 我们来简单看一下feature
`40:19` 分支只有一个C2
`40:20` 提交main分支有C三C四C53次提交
`40:24` 现在我需要把main分支合并进feature分支
`40:30` 我使用REBASE命令
`40:31` 那就是这个样
`40:32` git rebase后面还是分支的名字
`40:35` 那就是main分支
`40:36` 这个瑞贝斯命令有两层的理解
`40:38` 从最终达成的效果来看
`40:40` 可以认为是把main分支合并进了feature分支
`40:43` 第二层理解是把feature分支变基到了main分支
`40:47` 这里的REBASE就是变基
`40:49` 也就是更改地基
`40:51` 我们把这个命令敲下去看一下
`40:53` 可以看到feature分支整个就消失了
`40:56` 这个C2作为一次新的提交
`40:58` 变成C21撇挂到了下面
`41:00` 这个就是变基
`41:02` 他的意思是说我自己的这根线
`41:04` 也就是我的根基不存在了
`41:07` 我把我的根基变更到了main分支上
`41:10` 然后我把C2提交新生成一个C21撇
`41:13` 这个操作就叫做变基
`41:15` 从最终达成的效果来看
`41:17` 可以认为是把main分支合并进了feature分支
`41:20` 第二层理解是把feature分支变到了main分支
`41:23` 这里还是进入需要被合并的分支
`41:25` 还是feature分支
`41:27` 我们点击下面的create emerge
`41:30` 然后这里选择REBASE就是变机好
`41:33` 我们点击REBASE
`41:34` 这里给了一个提示
`41:36` 如果你使用便捷操作的话
`41:38` 你必须强制推送到GITHUB网站
`41:41` 就是git push杠F命令
`41:43` 也就是强制推送好
`41:45` 我们先不管这个提示
`41:46` 直接begin rebase好
`41:48` 辩机结束
`41:49` 我们来看一下历史记录
`41:50` 可以看到3452这个提交都在这一个分支上
`41:56` 而且这一次没有再创建这个merge
`41:58` 某某某某某commit的这么一个提交记录了
`42:02` 这个也就是瑞贝斯变机最大的一个好处
`42:04` 就是它不会创建那种merge
`42:06` 什么什么什么的那种提交记录
`42:08` 会让这个历史数变得更加干净
`42:11` 经过变机操作以后
`42:12` 必须强制推送到远端
`42:15` 而不能使用普通的推送了
`42:16` 它的根基发生了变化
`42:18` 只能使用强制推送
`42:20` 那这里我们把他强推上去
`42:22` 然后点击im sure好
`42:24` 这就强制推送成功了
`42:26` 强制推送只适合于这种
`42:28` 只有你一个人使用的分支
`42:30` 如果这个分支是多人协作分支
`42:33` 也就是很多个同事往同一个分支上面提交代码
`42:37` 这时候是万万不能使用强制推送的
`42:39` 你使用强制推送
`42:41` 有可能会把别人写的代码给覆盖掉
`42:43` 把别人写的代码搞丢好
`42:45` 这就是本期视频全部内容了
`42:47` 感谢大家点赞支持
`42:48` 我们下期再见